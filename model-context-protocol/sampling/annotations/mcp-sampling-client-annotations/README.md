# NestJS AI MCP Sampling Client Annotations

This package is the annotation-based NestJS AI translation of the Spring AI MCP sampling client example.

It handles `@McpSampling`, `@McpLogging`, and `@McpProgress` requests for the matching server package.

## Run

```bash
pnpm install
pnpm start
```
